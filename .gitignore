node_modules/
dist/
*.log
.DS_Store

# Desktop build inputs, both generated by desktop/scripts/stage.mjs from the app
# build and the brand icons.
desktop/renderer/
desktop/build/

# Never commit a real vault. The only checked-in vaults are the published demo
# and format-contract vectors.
*.keyhole.json
!examples/demo-vault.keyhole.json
!vectors/demo-vault.keyhole.json

# Packed vector release assets (regenerate with npm run pack:vectors)
vectors/dist/
