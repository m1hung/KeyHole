# @keyhole/shared

Sync HTTP client and shared React UI used by **keyhole-desktop** and
**keyhole-extension**.

Published from the desktop repo. Consumers:

```bash
npm install @keyhole/shared
# styles
import '@keyhole/shared/styles.css';
```
