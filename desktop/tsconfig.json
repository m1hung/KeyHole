{
  "//": "The desktop process is plain JS, but it is the one part of Keyhole with filesystem and IPC access — so it is type-checked as strictly as the TypeScript workspaces, using Electron's bundled type definitions.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2022", "DOM"],
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": true,
    "noUnusedLocals": true,
    "noImplicitReturns": true,
    "skipLibCheck": true,
    "types": ["node"]
  },
  "include": ["src/**/*.js", "src/**/*.cjs", "scripts/**/*.mjs"]
}
