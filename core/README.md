# @keyhole/core

Framework-agnostic crypto + vault core for Keyhole. No I/O.

Published from **keyhole-desktop**. Same sealed envelope format as Swift
`KeyholeCore` in **keyhole-ios** — interchange via `vectors/`.
