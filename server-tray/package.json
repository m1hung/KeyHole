{
  "name": "@keyhole/server-tray",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Keyhole sync server as a one-click Windows tray app",
  "author": "Keyhole",
  "license": "MIT",
  "main": "src/main.js",
  "scripts": {
    "bundle": "node scripts/bundle.mjs",
    "start": "npm run bundle && electron .",
    "build": "npm run bundle && electron-builder --win portable",
    "//test": "Covered by @keyhole/server's own 26 tests (this workspace only launches that code) plus typecheck and the manual checklist.",
    "test": "node -e \"console.log('@keyhole/server-tray: no unit tests (server logic is tested in @keyhole/server)')\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "0.25.12",
    "electron": "43.2.0",
    "electron-builder": "26.15.3"
  }
}
