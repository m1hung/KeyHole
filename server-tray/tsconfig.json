{
  "//": "Same rule as @keyhole/desktop: the main process is plain JS, but it spawns processes and owns the bind address, so it is type-checked as strictly as the TypeScript workspaces using Electron's own definitions.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2022", "DOM"],
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": true,
    "noUnusedLocals": true,
    "noImplicitReturns": true,
    "skipLibCheck": true,
    "types": ["node"]
  },
  "include": ["src/**/*.js", "scripts/**/*.mjs"]
}
